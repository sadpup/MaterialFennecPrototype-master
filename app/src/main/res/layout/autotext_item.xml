<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:padding="@dimen/autotest_item_space">

    <ImageView
        android:id="@+id/autotext_imageView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/search"
        android:layout_gravity="left"/>
    <TextView
        android:id="@+id/autotext_msg_textview"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:text="msg"
        android:layout_marginLeft="@dimen/autotest_item_space"/>
    <TextView
        android:id="@+id/autotext_up_textview"
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:text="↑"
        android:gravity="right"
        android:layout_marginRight="@dimen/autotest_item_space"/>

</LinearLayout>
